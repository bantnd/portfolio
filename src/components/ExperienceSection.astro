---
import { about } from "../consts";
---

<section>
  <div class="mb-10 px-2 md:px-8">
    <h2 class="mb-2">
      <span class="fancy-highlight">Work Experience</span>
    </h2>
    <p class="text-muted text-sm uppercase tracking-widest font-bold opacity-60">Professional Journey</p>
  </div>
  <div class="flex flex-col gap-10">
    {
      about.workExperience.map((exp) => (
        <div class="group relative rounded-3xl border border-base-content/10 bg-base-content/[0.03] p-8 shadow-2xl transition-all duration-300 hover:border-accent/30 hover:bg-base-content/[0.07] hover:shadow-accent/5">
          <div class="flex flex-col justify-between gap-4 md:flex-row md:items-start">
            <div>
              <h3 class="text-2xl font-bold tracking-tight text-base-content transition-colors group-hover:text-accent">{exp.title}</h3>
              <p class="mt-2 flex items-center gap-2 text-sm font-medium text-accent/90">
                {exp.company}
                <span class="h-1 w-1 rounded-full bg-accent/40"></span>
                <span class="text-muted/80">{exp.location}</span>
              </p>
            </div>
            <div class="rounded-full border border-accent/20 bg-accent/5 px-4 py-1.5 text-xs font-semibold tracking-wider text-accent uppercase w-fit shrink-0">
              {exp.timeline}
            </div>
          </div>
          <div class="mt-6">
            {Array.isArray(exp.description) ? (
              <ul class="text-muted list-disc space-y-2 pl-5 text-sm leading-relaxed">
                {exp.description.map((item) => (
                  <li
                    set:html={item.replace(
                      /\*\*(.*?)\*\*/g,
                      '<strong class="text-base-content font-semibold">$1</strong>',
                    )}
                  />
                ))}
              </ul>
            ) : (
              <p
                class="text-muted text-sm leading-relaxed"
                set:html={exp.description.replace(
                  /\*\*(.*?)\*\*/g,
                  '<strong class="text-base-content font-semibold">$1</strong>',
                )}
              />
            )}
          </div>
        </div>
      ))
    }
  </div>
</section>
